import React, { Component } from 'react';
import Workspace from './views/Workspace';

class App extends Component {
  render() {
    return (
      <div>
        <Workspace />
      </div>
    );
  }
}

export default App;
